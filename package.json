{
  "name": "biobridge-lab",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "tsc && vite build && mkdir -p .build-outputs && cp -R dist/. .build-outputs/ && (cp assets/app-debug.apk .build-outputs/app-debug.apk || true) && mkdir -p app/build/outputs/apk/debug && (cp assets/app-debug.apk app/build/outputs/apk/debug/app-debug.apk || true) && mkdir -p build/outputs/apk/debug && (cp assets/app-debug.apk build/outputs/apk/debug/app-debug.apk || true)",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@google/genai": "^2.4.0",
    "lucide-react": "^0.546.0",
    "motion": "^12.23.24",
    "react": "^19.0.1",
    "react-dom": "^19.0.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.14",
    "@types/react": "^19.0.1",
    "@types/react-dom": "^19.0.1",
    "@vitejs/plugin-react": "^5.0.4",
    "autoprefixer": "^10.4.21",
    "tailwindcss": "^4.1.14",
    "typescript": "~5.8.2",
    "vite": "^6.2.3"
  }
}
